import PatternNav from "../components/Pattern/patternNav"; // Correct casing

const Pattern = () => {
  return (
    <div>
      Pattern
      <PatternNav /> {/* Correct component usage */}
    </div>
  );
};

export default Pattern;
